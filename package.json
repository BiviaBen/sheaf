{
  "name": "sheaf-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end (browser) test harness for the Sheaf plugin. Dev-only; never shipped in the plugin zip.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "jszip": "^3.10.1"
  }
}
